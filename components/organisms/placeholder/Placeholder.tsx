import { Text, View, StyleSheet } from "react-native";

export interface Props {}

export default function PlaceHolder({ ...rest }: Props) {
  return <View></View>;
}

const styles = StyleSheet.create({});
